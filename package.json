{
  "name": "azure-queue-add-message",
  "version": "2.1.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "coverage": "nyc ava",
    "coveralls": "nyc ava && nyc report --reporter=lcov && cat coverage/lcov.info | coveralls",
    "test": "standard && npm audit && ava",
    "test-offline": "standard && ava",
    "standard-fix": "standard --fix",
    "refresh": "rm -rf node_modules && rm package-lock.json && npm install"
  },
  "keywords": [],
  "author": "Geir Gåsodden <geir.gasodden@pythonia.no> (https://github.com/zrrrzzt)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/telemark/azure-queue-add-message.git"
  },
  "bugs": {
    "url": "https://github.com/telemark/azure-queue-add-message/issues"
  },
  "homepage": "https://github.com/telemark/azure-queue-add-message#readme",
  "devDependencies": {
    "ava": "1.4.1",
    "coveralls": "3.0.3",
    "nyc": "14.1.0",
    "standard": "12.0.1"
  },
  "dependencies": {
    "azure-sb": "0.11.0"
  },
  "engines": {
    "node": ">=10.15.3"
  }
}
